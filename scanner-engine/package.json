{
  "name": "scanner-engine",
  "version": "1.0.0",
  "description": "Playwright-based dynamic scanner engine",
  "scripts": {
    "test": "npx playwright test",
    "test:api": "npx playwright test tests/api.health.spec.js",
    "test:report": "npx allure generate allure-results --clean -o allure-report"
  },
  "dependencies": {
    "axe-core": "^4.9.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "allure-playwright": "^3.0.0-beta.5",
    "allure-commandline": "^2.21.0"
  }
}
